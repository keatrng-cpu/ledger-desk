# Template for gateway/.env.local  (the real file is gitignored via .env.*)
#
#   Windows:   copy gateway\env.local.template gateway\.env.local
#   then fill the two blank values. One KEY=VALUE per line, no quotes needed.
#
# DATABASE_URL
#   The SAME Session Pooler string Netlify uses. Copy it whole from
#   Supabase -> project ledger-desk -> Connect -> Session pooler.
#   It has: user  postgres.nfwuvpsxnxctinywwrgh
#           host  aws-0-us-east-1.pooler.supabase.com   port 5432   db postgres
#   Never the db.<ref>.supabase.co host - it is IPv6-only and Netlify/this
#   PC cannot reach it.
#
# DATABENTO_API_KEY
#   Portal -> API keys -> Create new key (name it ledger-desk). Starts with db-
#   Must be on the Standard plan for live CME. Same key as the desk .env.
#   Verified 2026-09-14: Live yields ESU6/NQU6 ohlcv-1s with pretty_close.
# DATABASE_URL is required for the gateway process (ticks → Postgres).
# DATABENTO_DELAY_MINUTES=0 lives in the repo-root .env (desk historical), not here.

DATABASE_URL=
DATABENTO_API_KEY=
DATABENTO_DATASET=GLBX.MDP3
