# INTEGRATION-P0 — Truth & safety (branch agents/phase-p0)

1. Prod build fixed: created `scripts/install-page.html` ({{APP_NAME}}/{{APP_URL}} template) that `server/middleware/grok-pwa.ts` imports via `?raw` and the dev plugin reads at runtime — `npx vite build` (full Nitro/Vercel) now succeeds.
2. `build-desk.ts` now gates on data quality after `scanSetups`: any non-yahoo series or quote lag > 120s forces every candidate `actionable=false`, pushes a "Data quality: …" blocked reason, and overwrites `scan.focus`; the checklist `risk` item now reflects dataQualityOk + weekday (TODO in code points to INTEGRATION-P1.md for the real risk governor).
3. `session-hud.tsx` adds a worst-of-two-quotes lag chip (green ≤15s / amber ≤120s / red >120s) and a hard red "SYNTHETIC DATA" banner when any series/quote source is synthetic.
4. Score renamed in all user-visible copy to "pre-score" (scanner focus strings + setup-scanner header/card); `confluence` field name/type unchanged. Also: dead `peer` param removed from scanner `build()`, short path got the same +0.02 smt "locked" bonus as long, and `voteBias()` in structure.ts rewritten to plain majority-wins/tie-neutral (identical behavior — my only structure.ts edit).
5. Deleted unreferenced revenue dead code: `src/lib/data/sample-revenue.ts`, `src/lib/ai/analyze.ts`, dashboard `ai-insights/kpi-cards/trend-charts/breakdown-table/date-range-filter` (`src/lib/data` and `src/lib/ai` dirs are now gone; only aplus-ops, dual-index-charts, candlestick-pane remain and index.tsx needed no import changes). Integrator note: another agent owns the rest of structure.ts; the lag threshold (120s) is duplicated in HUD chip colors and build-desk gate — keep in sync if tuned.
